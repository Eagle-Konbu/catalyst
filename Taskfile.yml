version: '3'

tasks:
  build:
    vars:
      GOARCH: arm64
      GOOS: darwin
    cmds:
      - GOARCH={{.GOARCH}} GOOS={{.GOOS}} go build -o bin/catalyst main.go
    sources:
      - '**/*.go'
    generates:
      - bin/catalyst
